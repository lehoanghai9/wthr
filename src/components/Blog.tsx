import React from 'react'
import { Container, SectionContainer, SectionTitle } from './Container'

const Blog = () => {
  return (
    <SectionContainer id="blog">
      <Container>
        <SectionTitle label="Our latest stories" />
      </Container>
    </SectionContainer>
  )
}

export default Blog
